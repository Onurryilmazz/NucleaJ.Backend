package com.nuclea.common.service.marker;

/**
 * Marker interface for scoped (request-scoped) services.
 * Services implementing this interface will automatically be registered with @Scope("request") lifecycle.
 */
public interface IScopedService {
}
